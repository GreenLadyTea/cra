import Thermometer from "./Thermometer";

test('Показывает температуру синим, если ниже 0', () => {
    const temp = -30;

});

test('Показывает температуру красным, если выше 0', () => {
    const temp = 24;

});

test('Показывает температуру черным, если 0', () => {
    const temp = 0;
});
